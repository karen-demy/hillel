alert ('Я – JavaScript!');

let name = 'Ivan';
let admin ;
admin = name ;
alert (admin);

let nameForPlanet;
let curentUserName;

alert (`привіт ${1}`);
alert (`привіт ${"name"}`); 
alert (`привіт ${name}`); 

curentUserName = prompt('Як тебе звуть?');
alert (`Привіт, ${curentUserName}!Ми раді знайомству)`);